extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back to Safety
    article
        h1= error.message
        h3= `Error Code: ${error.status}`
        pre= error.stack 